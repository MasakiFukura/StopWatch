# 汎用マルチストップウォッチ仕様

## 概要

ブラウザで動作する、汎用的なマルチストップウォッチを作成する。

複数のストップウォッチを円形で表示し、用途に応じて名称を変更できるようにする。動作モードは、常に1つだけ動く「切り替えモード」と、複数を同時に動かせる「同時実行モード」を選択できる。

PCで開発し、GitHub Pagesなどの静的ホスティングでスマホから利用できる構成にする。

## 技術要件

- HTML、CSS、JavaScriptのみで実装する
- ビルド環境は不要にする
- 外部ライブラリは使用しない
- `index.html` 単体で動作する
- スマホ表示を前提にレスポンシブ対応する

## 画面構成

画面上部に使い方の説明と設定エリアを配置する。

使い方の説明については、本ページでどのようなことが可能かと、各設定の方法を記載する。
初見の人でも使用可能であること、複数のストップウォッチを利用したい人に検索で見つけてもらえるように心がける。

設定エリアには以下を置く。

- ストップウォッチ数
- ストップウォッチサイズ
- 配置の流れ
- 折り返し数
- 動作モード
- リセットボタン

その下に、ストップウォッチ一覧を表示する。

## 設定項目

### ストップウォッチ数

- スピンボックスで数値入力で指定する
  直接入力および上下ボタンによる変更を可能とする
- 初期値は `4`
- 最小値は `1`
- 最大値は `20`
- 値を変更すると、表示するストップウォッチ数を変更する
- 新しく増えたストップウォッチの名称は `ストップウォッチ1`、`ストップウォッチ2` のようにする

### ストップウォッチサイズ

- スピンボックスで数値入力で指定する
  直接入力および上下ボタンによる変更を可能とする
- 単位は `px`
- 円形ストップウォッチの直径として扱う
- 初期値は `160`
- 最小値は `10`
- 最大値は `1000`

### 配置の流れ

ラジオボタンで選択する。

選択肢は以下とする。

- 横方向優先
- 縦方向優先

初期値は `横方向優先` とする。

#### 横方向優先

左から右へ並べる。

折り返し数に達したら、次の行に配置する。

例：折り返し数が `2` の場合

```text
1 2
3 4
5 6
```

#### 縦方向優先

上から下へ並べる。

折り返し数に達したら、次の列に配置する。

例：折り返し数が `2` の場合

```text
1 3 5
2 4 6
```

### 折り返し数

- 数値入力で指定する
- 初期値は `2`
- 最小値は `1`
- 最大値は `10`
- 横方向優先の場合は、1行あたりの表示数として扱う
- 縦方向優先の場合は、1列あたりの表示数として扱う

### 動作モード

ラジオボタンで選択する。

選択肢は以下とする。

- 切り替えモード
- 同時実行モード

初期値は `切り替えモード` とする。

## ストップウォッチ表示

各ストップウォッチは以下で構成する。

- 名称入力欄
- 色変更ボタン
- 円形のストップウォッチ本体
- 円の中に経過時間を表示する

名称入力欄と色変更ボタンは円の上に配置する。
色変更ボタン押下でカラーパレットが開き、ストップウォッチが選択した色に変わる。
円形ストップウォッチ本体は、円全体をクリックまたはタップ可能にする。
動作中のストップウォッチは、停止中と見分けがつくように表示を変える。

## 時間表示

時間は以下の形式で表示する。

```text
mm:ss.x
```

1時間以上になった場合は以下の形式で表示する。

```text
hh:mm:ss.x
```

表示更新間隔は100ms

## 動作仕様

### 切り替えモード

停止中のストップウォッチを押した場合。

- 押したストップウォッチを開始する
- 他のストップウォッチはすべて停止する

動作中のストップウォッチを押した場合。

- 押したストップウォッチを停止する

### 同時実行モード

停止中のストップウォッチを押した場合。

- 押したストップウォッチを開始する
- 他のストップウォッチの状態は変更しない

動作中のストップウォッチを押した場合。

- 押したストップウォッチだけ停止する

## リセット

リセットボタンを押すと、以下を実行する。

- すべてのストップウォッチを停止する
- すべての経過時間を `0` に戻す
- 名称はリセットしない
- ストップウォッチ数や配置設定もリセットしない

## スクロール

ストップウォッチが画面内に収まらない場合は、画面をスクロールできるようにする。

横方向にも縦方向にもはみ出す可能性があるため、必要に応じてスクロールできるようにする。

## 保存

初期実装では保存機能は不要とする。

ページを再読み込みした場合、設定や計測時間は初期状態に戻ってよい。

## 実装ファイル

以下の1ファイル構成にする。

```text
index.html
```

`index.html` 内に、HTML、CSS、JavaScriptをすべて記載する。

## コーディング方針

- 関数にはJSDoc形式のコメントを付ける
- 変数名、関数名は役割がわかる名前にする
- 計測時間はミリ秒で管理する
- 表示更新は `setInterval` で行う
- 実際の経過時間は `Date.now()` を基準に計算する
- 表示更新間隔に依存して時間がずれないようにする
